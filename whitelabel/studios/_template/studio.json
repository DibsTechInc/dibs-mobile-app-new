{
  "//": "Copy this directory to whitelabel/studios/<slug>/ and fill it in. Run `npm run whitelabel:validate` after editing. Schema + field docs: whitelabel/schema.ts. Store-account fields stay empty until the app has a listing; development builds work without them, release builds refuse.",

  "slug": "example-studio",
  "dibsStudioId": 12345,
  "appName": "Example Studio",
  "shortName": "Example",
  "accentColor": "#356280",
  "timezone": "America/New_York",

  "//store": "ownership 'dibs' + releaseType 'update' = ship into an EXISTING Dibs-owned listing (bundleId must match it exactly). ownership 'studio' + releaseType 'new' = fresh listing under the studio's own account, per Apple 4.2.6.",
  "store": {
    "ownership": "studio",
    "releaseType": "new",
    "platforms": ["ios"],
    "version": "1.0.0",
    "buildNumber": 1
  },

  "//features": "One bookable surface per app today: classes OR appointments. roomBooking = the appointments are rooms, not people (skips the provider step; needs appointments.providerByServiceId). monthlyCommitment = a chosen slot offers the Single / Lock-in-monthly toggle.",
  "features": {
    "classes": true,
    "appointments": false,
    "roomBooking": false,
    "monthlyCommitment": false
  },

  "//appointments": "Only needed when features.appointments is true. availabilityVariant 'custom-263' exists solely for studio 263's server-side endpoint. providerByServiceId maps appointment_type id -> the instructor id every booking of that type carries (required for roomBooking studios).",
  "appointments": {
    "availabilityVariant": "standard",
    "providerByServiceId": {}
  },

  "display": {
    "showInstructor": true
  },

  "ios": {
    "bundleId": "com.examplestudio.app",
    "appleTeamId": "",
    "ascAppId": "",
    "merchantId": "merchant.com.examplestudio.app"
  },
  "android": {
    "package": "com.examplestudio.app"
  },

  "support": {
    "email": "hello@examplestudio.com"
  },
  "legal": {
    "//privacyPolicyLive": "Flip to true only after LOADING the URL and seeing a policy. Every path on dibsonline.com returns HTTP 200 with the widget SPA shell, so a status code proves nothing.",
    "privacyPolicyUrl": "https://dibsonline.com/apple/privacy-policy",
    "privacyPolicyLive": false,
    "supportUrl": "https://examplestudio.com/contact"
  },

  "api": {
    "url": "https://api.dibsonline.com/api/v2"
  },

  "assets": {
    "logo": "assets/logo.png",
    "hero": "assets/hero.png",
    "iconSource": "assets/icon-source.png",
    "splash": "assets/splash.png"
  }
}
